name: Build HAPP routing

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "shadowrocket.conf"
      - "rules/*.list"
      - "distillate/**"
      - "scripts/sync_lists.py"
      - "scripts/build_distillate.py"
      - "scripts/build_happ_routing.py"
      - ".github/workflows/sync-lists.yml"
      - ".github/workflows/build-happ-routing.yml"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Build HAPP artifacts
        run: python scripts/build_happ_routing.py

      - name: Commit and push changes
        run: |
          set -euo pipefail
          if [[ -n "$(git status --porcelain HAPP)" ]]; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add HAPP/*
            git commit -m "chore: rebuild happ artifacts from vendored distillate"
            git push
          else
            echo "No changes to commit."
          fi
